# Servidor
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Base de datos (rehabmano)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=rehabmano

# Autenticación
JWT_SECRET=cambiar_este_secreto
JWT_EXPIRES_IN=1h

# Órtesis (a partir del sprint 3). SERIAL_MOCK=true simula lecturas sin hardware.
SERIAL_PORT=COM4
SERIAL_MOCK=true
